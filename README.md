# my-workout-planner
Version 4.1 — PWA/Home Screen update.
Keeps Version 4 functionality and adds manifest, iPhone web-app metadata, icons and service worker.
Deploy ALL files to HTTPS/GitHub Pages. Then open the site in Safari → Share → Add to Home Screen → Open as Web App → Add.
A ZIP itself cannot be installed directly as a Home Screen app; it must first be deployed as a website.
